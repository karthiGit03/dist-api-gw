version: '3.9'

services:

  redis:
    image: redis
    ports:
      - "6379:6379"

  backend1:
    image: test-2-app
    ports:
      - "9091:9090"
    depends_on:
      - redis

  backend2:
    image: test-2-app
    ports:
      - "9092:9090"
    depends_on:
      - redis

  backend3:
    image: test-2-app
    ports:
      - "9093:9090"
    depends_on:
      - redis